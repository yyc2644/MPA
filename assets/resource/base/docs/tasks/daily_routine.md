# 每日流程

每日流程按照前端中启用的开关，依次执行启动、礼物箱、免费卡包、得卡挑战、商店免费项和任务奖励。

该功能仍包含骨架或不完整子流程，当前已从 MFAAvalonia 任务列表隐藏，玩家无法勾选或执行。补充真实识别资源时，应先对单个任务完成截图回放测试，再恢复对应前端任务和选项。

每日步骤通过 MaaFramework 的 `[JumpBack]` 和 `max_hit: 1` 依次执行，不会再把 `next` 候选列表误当成普通的顺序调用列表。
