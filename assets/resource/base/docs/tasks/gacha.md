# 自动抽卡

自动抽卡用于打开可用免费卡包。

在前端展开任务配置后，可以从“目标扩充包”中选择要打开的卡包，默认值为 `B3b · 奇迹日常`。

当前共配置 19 个正式扩充包：A 系列 11 个（A1 至 A4b）和 B 系列 8 个（B1 至 B3b）。PROMO 活动卡包不属于常规开包入口，因此不提供选择。

流水线会按配置切换 A/B 系列、识别对应扩充包名称，并在进入卡包后用扩充包编号进行二次确认。旧扩充包不在首屏时会自动向下滚动查找。
